<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/dialog_background"
    android:paddingBottom="@dimen/dialog_paddingtop"
    android:paddingLeft="@dimen/dialog_paddingleft"
    android:paddingRight="@dimen/dialog_paddingleft"
    android:paddingTop="@dimen/dialog_paddingtop" >

    <ProgressBar
        android:id="@+id/dialog_progress"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true" />

    <TextView
        android:id="@+id/dialog_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/dialog_marginleft"
        android:layout_toRightOf="@id/dialog_progress"
        android:text="@string/login_loading"
        android:textColor="@color/white"
        android:textSize="@dimen/login_button_textsize" />

</RelativeLayout>